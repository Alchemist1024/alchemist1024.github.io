<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>conda使用教程</title>
    <url>/2020/08/10/conda%E4%BD%BF%E7%94%A8%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><ul>
<li>开源包管理系统和环境管理系统 ，包括多种语言的包安装，运行，更新，删除，最重要的是可以解决包依赖问题</li>
<li>支持语言包括 Python，R，Ruby，Lua，Scala，Java，JavaScript，C / C ++，FORTRAN</li>
<li>支持在Windows，macOS和Linux上运行</li>
<li>conda可以构建不同的环境，同时可以对环境进行保存，加载和切换操作</li>
<li>conda包和环境管理器包含在所有版本的Anaconda和Miniconda中</li>
</ul>
<a id="more"></a>

<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><ul>
<li><p><strong>下载 (Linux 64位系统为例)</strong></p>
<p><a href="https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh">https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh</a></p>
<p>下载地址：<a href="https://conda.io/en/latest/miniconda.html">https://conda.io/en/latest/miniconda.html</a></p>
<p>清华镜像：<a href="https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/">https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/</a></p>
</li>
<li><p><strong>安装</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash Miniconda3-latest-Linux-x86_64.sh</span><br></pre></td></tr></table></figure>
</li>
<li><p>查看是否安装成功,如果安装没问题会显示conda版本号</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda --version</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h2><ul>
<li><code>conda list</code> 查看安装了哪些包。</li>
<li><code>conda env list</code> 或 <code>conda info -e </code>查看当前存在哪些虚拟环境</li>
<li><code>conda update conda</code> 检查更新当前conda</li>
</ul>
<h2 id="管理虚拟环境"><a href="#管理虚拟环境" class="headerlink" title="管理虚拟环境"></a>管理虚拟环境</h2><h3 id="创建虚拟环境"><a href="#创建虚拟环境" class="headerlink" title="创建虚拟环境"></a>创建虚拟环境</h3><p>使用 <code>conda create -n your_env_name python=X.X（2.7、3.6等)</code>命令创建<code>python</code>版本为<code>X.X</code>、名字为<code>your_env_name</code>的虚拟环境。<code>your_env_name</code>文件可以在<code>Anaconda</code>安装目录<code>envs</code>文件下找到。</p>
<h3 id="激活虚拟环境"><a href="#激活虚拟环境" class="headerlink" title="激活虚拟环境"></a>激活虚拟环境</h3><p> <strong>Linux:</strong><code> conda activate your_env_name(虚拟环境名称)</code></p>
<p><strong>Windows:</strong><code> activate your_env_name(虚拟环境名称)</code></p>
<h3 id="对虚拟环境中安装额外的包"><a href="#对虚拟环境中安装额外的包" class="headerlink" title="对虚拟环境中安装额外的包"></a>对虚拟环境中安装额外的包</h3><p>使用命令<code>conda install -n your_env_name [package]</code>即可安装<code>package</code>到<code>your_env_name</code>中</p>
<h3 id="关闭虚拟环境"><a href="#关闭虚拟环境" class="headerlink" title="关闭虚拟环境"></a>关闭虚拟环境</h3><p> <strong>Linux:</strong> <code>conda deactivate</code></p>
<p> <strong>Windows:</strong> <code>deactivate</code></p>
<h3 id="删除虚拟环境"><a href="#删除虚拟环境" class="headerlink" title="删除虚拟环境"></a>删除虚拟环境</h3><p>使用命令<code>conda remove -n your_env_name(虚拟环境名称) --all</code>， 即可删除。</p>
<h3 id="删除环境中的某个包"><a href="#删除环境中的某个包" class="headerlink" title="删除环境中的某个包"></a>删除环境中的某个包</h3><p>使用命令<code>conda remove --name your_env_name package_name</code> 即可</p>
<h2 id="环境迁移"><a href="#环境迁移" class="headerlink" title="环境迁移"></a>环境迁移</h2><h3 id="Clone"><a href="#Clone" class="headerlink" title="Clone"></a>Clone</h3><p>假如前提是，在本地的conda里已经有一个AAA的环境，我想创建一个新环境跟它一模一样的叫BBB，那么这样一句就搞定了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create -n BBB --<span class="built_in">clone</span> AAA</span><br></pre></td></tr></table></figure>

<p>但是如果是跨计算机呢。</p>
<p>查询conda create命令的原来说明，是这样的：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">–<span class="built_in">clone</span> ENV </span><br><span class="line">Path to (or name of) existing <span class="built_in">local</span> environment.</span><br></pre></td></tr></table></figure>

<p><code>–clone</code>这个参数后面的不仅可以是环境的名字，也可以是环境的路径。</p>
<p>所以，很自然地，我们可以把原来电脑上目标conda环境的目录复制到新电脑上，然后再用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create -n BBB --<span class="built_in">clone</span> ~/path</span><br></pre></td></tr></table></figure>

<p>就直接一步安装了所有的包，完成了环境的移植。 </p>
<p><strong>note</strong>: 移植过来的环境只是安装了你原来环境里用<code>conda install</code>等命令直接安装的包，你用<code>pip</code>之类装的东西没有移植过来，需要你重新安装。</p>
<h3 id="Spec-List"><a href="#Spec-List" class="headerlink" title="Spec List"></a>Spec List</h3><p>如果需要在具有 <strong>相同操作系统</strong> 的计算机之间复制环境，则可以生成 <code>spec list</code>。</p>
<p><strong>生成 <code>spec list</code> 文件：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda list --explicit &gt; spec-list.txt</span><br></pre></td></tr></table></figure>

<p><strong>重现环境：</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create  --name python-course --file spec-list.txt</span><br></pre></td></tr></table></figure>

<h3 id="Environment-yml-本人常用​​"><a href="#Environment-yml-本人常用​​" class="headerlink" title="Environment.yml(本人常用​​)"></a>Environment.yml(本人常用​​)</h3><p>使用 <code>-export</code> 选项生成一个 <code>environment.yml</code> 文件，以在 <strong>不同的平台和操作系统之间</strong> 复现项目环境。 <code>spec list</code> 文件和 <code>environment.yml</code> 文件之间的区别在于： <code>environment.yml</code> 文件不针对特定操作系统，并且使用YAML格式。 <code>environment.yml</code> 仅列出了软件包名称，由 conda 基于软件包的名称构建环境。 另一个区别是 <code>-export</code> 还包括使用pip安装的软件包，而 <code>spec list</code> 则没有。</p>
<h4 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h4><p><strong>进入py3环境</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda activate py3</span><br></pre></td></tr></table></figure>

<p><strong>运行命令</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda env <span class="built_in">export</span> &gt; environment.yml</span><br></pre></td></tr></table></figure>

<p>会在当前目录的文件夹下生成 <code>environment.yml</code>文件，复制文件到新的机器上。</p>
<p><strong>查看文件</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name: py3</span><br><span class="line">channels:</span><br><span class="line">- r</span><br><span class="line">- defaults</span><br><span class="line">dependencies:</span><br><span class="line">- openssl=1.0.2l=0</span><br><span class="line">- pip=9.0.1=py36_1</span><br><span class="line">- python=3.6.2=0</span><br><span class="line">- readline=6.2=2</span><br><span class="line">- setuptools=27.2.0=py36_0</span><br><span class="line">- sqlite=3.13.0=0</span><br><span class="line">- tk=8.5.18=0</span><br><span class="line">- wheel=0.29.0=py36_0</span><br><span class="line">- xz=5.2.3=0</span><br><span class="line">- zlib=1.2.11=0</span><br><span class="line">prefix: /home/dpuser/miniconda3/envs/py3</span><br></pre></td></tr></table></figure>

<p><strong>重现环境</strong></p>
<p>进入到新机器的<code>enviroment.yml</code>保存路径运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda env create -f environment.yml</span><br></pre></td></tr></table></figure>

<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://zhuanlan.zhihu.com/p/89356758">Conda安装使用详解</a></p>
<p><a href="https://blog.csdn.net/lyy14011305/article/details/59500819">用conda创建python虚拟环境</a></p>
<p><a href="https://blog.csdn.net/qq_35860352/article/details/80685175">Conda环境移植（克隆）的方法</a></p>
<p><a href="https://zhuanlan.zhihu.com/p/87344422">Conda环境迁移</a></p>
<p><a href="http://pre.tir.tw/008/blog/output/conda_yml.html">conda套件管理environment.yml</a></p>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
      <tags>
        <tag>conda</tag>
        <tag>虚拟环境</tag>
      </tags>
  </entry>
  <entry>
    <title>python规范</title>
    <url>/2020/08/15/python%E8%A7%84%E8%8C%83/</url>
    <content><![CDATA[<h2 id="注释规范"><a href="#注释规范" class="headerlink" title="注释规范"></a>注释规范</h2><h3 id="类注释"><a href="#类注释" class="headerlink" title="类注释"></a>类注释</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SampleClass</span>:</span></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;基本描述</span></span><br><span class="line"><span class="string">    详细描述</span></span><br><span class="line"><span class="string">    Attributes:</span></span><br><span class="line"><span class="string">    	likes_spam:</span></span><br><span class="line"><span class="string">    	eggs:</span></span><br><span class="line"><span class="string">    &#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, like_spam=False</span>):</span></span><br><span class="line">        <span class="string">&#x27;&#x27;&#x27;Inits SampleClass with blah.&#x27;&#x27;&#x27;</span></span><br><span class="line">        self.like_spam = like_spam</span><br><span class="line">        self.eggs = <span class="number">0</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="方法注释"><a href="#方法注释" class="headerlink" title="方法注释"></a>方法注释</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sample_method</span>():</span></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;基本描述</span></span><br><span class="line"><span class="string">    详细描述</span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">    	a:</span></span><br><span class="line"><span class="string">    	b:</span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">    	a:</span></span><br><span class="line"><span class="string">    	b:</span></span><br><span class="line"><span class="string">    Raises:</span></span><br><span class="line"><span class="string">    	可能的异常</span></span><br><span class="line"><span class="string">    &#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure>

<h2 id="命名规范"><a href="#命名规范" class="headerlink" title="命名规范"></a>命名规范</h2><h3 id="模块名"><a href="#模块名" class="headerlink" title="模块名"></a>模块名</h3><p>模块应该使用尽可能短的、全小写命名，可以在模块命名时使用下划线以增强可读性。同样包的命名也应该是这样的，虽然其并不鼓励下划线。</p>
<p>主要是考虑模块名是与文件夹相对应的，因此需要考虑文件系统的一些命名规则的，比如Unix系统对大小写敏感，而过长的文件名会影响其在 Windows\Mac\Dos 等系统中的正常使用。</p>
<p><strong>通常是小写字母，单词之间用_分割</strong></p>
<h3 id="包名"><a href="#包名" class="headerlink" title="包名"></a>包名</h3><p>和模块名一样。</p>
<h3 id="文件名"><a href="#文件名" class="headerlink" title="文件名"></a>文件名</h3><p>全小写,可使用下划线。</p>
<p><strong>注意</strong>：混合大小写仅被允许用于这种风格已经占据优势的时候，以便保持向后兼容。</p>
<h3 id="类名"><a href="#类名" class="headerlink" title="类名"></a>类名</h3><p>几乎毫无例外的，类名都使用首字母大写开头(Pascal命名风格)的规范。</p>
<p>使用 <code>_</code>单下划线开头的类名为内部使用</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> module_name <span class="keyword">import</span> *</span><br></pre></td></tr></table></figure>

<p>默认不被导入的情况 <code>_InnerClass</code></p>
<p><strong>单词首字母大写(Pascal命名风格)。</strong></p>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><h4 id="全局变量"><a href="#全局变量" class="headerlink" title="全局变量"></a>全局变量</h4><p>大写字母，单词之间用 <code>_</code>分割</p>
<p>对于 <code>from module_name import *</code>导入语句，如果想阻止导入模块内的全局变量可以使用旧有的规范，在全局变量上加一个前导的下划线。</p>
<p><strong>注意</strong>:应避免使用全局变量</p>
<h4 id="普通变量"><a href="#普通变量" class="headerlink" title="普通变量"></a>普通变量</h4><p>小写字母，单词之间用 <code>_</code>分割</p>
<p><strong>注意</strong>：</p>
<ol>
<li>不论是类成员变量还是全局变量，均不使用 m 或 g 前缀(匈牙利标记法)。</li>
<li>私有类成员使用单一下划线前缀标识，多定义公开成员，少定义私有成员。</li>
<li>变量名不应带有类型信息，因为Python是动态类型语言。如 <del>iValue、names_list、dict_obj</del> 等都是不好的命名</li>
</ol>
<h4 id="实例变量"><a href="#实例变量" class="headerlink" title="实例变量"></a>实例变量</h4><p>以 <code>_</code> 开头，其他和普通变量一样</p>
<h4 id="私有实例变量"><a href="#私有实例变量" class="headerlink" title="私有实例变量"></a>私有实例变量</h4><p>以 <code>__</code>开头（2个下划线），其他和普通变量一样</p>
<h4 id="专有变量"><a href="#专有变量" class="headerlink" title="专有变量"></a>专有变量</h4><p><code>__</code>开头，<code>__</code>结尾，一般为python的自有变量，</p>
<p><font color=red>不要以这种方式命名普通变量，这个是保留字，要满足防御性编程需求</font></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">__doc__</span><br><span class="line">__class__</span><br></pre></td></tr></table></figure>

<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><h4 id="普通函数"><a href="#普通函数" class="headerlink" title="普通函数"></a>普通函数</h4><p>和普通变量一样</p>
<h4 id="私有函数"><a href="#私有函数" class="headerlink" title="私有函数"></a>私有函数</h4><p>以 <code>__</code> 开头（2个下划线），其他和普通函数一样</p>
<h4 id="函数和方法的参数"><a href="#函数和方法的参数" class="headerlink" title="函数和方法的参数"></a>函数和方法的参数</h4><p><strong>总使用“self”作为实例方法的第一个参数。</strong><br><strong>总使用“cls”作为类方法的第一个参数。</strong></p>
<p>如果一个函数的参数名称和保留的关键字冲突，通常使用一个后缀下划线好于使用缩写或奇怪的拼写。</p>
<h3 id="常量"><a href="#常量" class="headerlink" title="常量"></a>常量</h3><p>常量名所有字母大写，由下划线连接各个单词如 <code>MAX_OVERFLOW</code>，<code>TOTAL</code></p>
<h3 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h3><p>因为异常也是一个类，所以遵守类的命名规则，</p>
<p>此外，如果异常实际上指代一个错误的话，应该使用“Error”做后缀。</p>
<h3 id="缩写"><a href="#缩写" class="headerlink" title="缩写"></a>缩写</h3><p>命名应当尽量使用全拼写的单词，缩写的情况有如下两种：</p>
<ol>
<li>常用的缩写，如<code>XML</code>、<code>ID</code>等，在命名时也应只大写首字母，如<code>XmlParser</code></li>
<li>命名中含有长单词，对某个单词进行缩写。这时应使用约定成俗的缩写方式</li>
</ol>
<p>例如：</p>
<table>
<thead>
<tr>
<th>名称</th>
<th>缩写</th>
</tr>
</thead>
<tbody><tr>
<td>function</td>
<td>fn</td>
</tr>
<tr>
<td>text</td>
<td>txt</td>
</tr>
<tr>
<td>object</td>
<td>obj</td>
</tr>
<tr>
<td>count</td>
<td>cnt</td>
</tr>
<tr>
<td>number</td>
<td>num</td>
</tr>
</tbody></table>
<h3 id="前导后缀下划线总结"><a href="#前导后缀下划线总结" class="headerlink" title="前导后缀下划线总结"></a>前导后缀下划线总结</h3><ul>
<li>一个前导下划线：表示非公有。</li>
<li>一个后缀下划线：避免关键字冲突。</li>
<li>两个前导下划线：当命名一个类属性引起名称冲突时使用，定义私有变量。</li>
<li>两个前导和后缀下划线：“魔术”（有特殊用途）对象或者属性，例如<strong>init</strong>或者<strong>file</strong>。绝对不要创造这样的名字，而只是使用它们。</li>
</ul>
<p>Python 用下划线作为变量前缀和后缀指定特殊变量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 不能用&#x27;from module import *&#x27;导入（相当于 private）</span></span><br><span class="line">_xxx</span><br><span class="line"><span class="comment"># 系统定义名字</span></span><br><span class="line">__xxx__</span><br><span class="line"><span class="comment"># 类中的私有变量名(相当于protect)</span></span><br><span class="line">__xxx</span><br></pre></td></tr></table></figure>

<h3 id="下划线核心风格"><a href="#下划线核心风格" class="headerlink" title="下划线核心风格"></a>下划线核心风格</h3><p><font color=red>避免用下划线作为变量名的开始</font></p>
<p>因为下划线对解释器有特殊的意义，而且是内建标识符所使用的符号，我们建议程序员避免用下划线作为变量名的开始。</p>
<p>一般来讲，变量名<code>_xxx</code>被看作是“私有的”，在模块或类外不可以使用。当变量是私有的时候，用<code>_xxx</code> 来表示变量是很好的习惯。</p>
<p> “单下划线” 开始的成员变量叫做保护变量，意思是只有类对象和子类对象自己能访问到这些变量；</p>
<p> “双下划线” 开始的是私有成员，意思是只有类对象自己能访问，连子类对象也不能访问到这个数据。</p>
<p>以单下划线开头<code>_foo</code>的代表不能直接访问的类属性，需通过类提供的接口进行访问，不能用<code>“from xxx import *”</code>而导入；<br>以双下划线开头的<code>__foo</code>代表类的私有成员；<br>以双下划线开头和结尾的<code>__foo__</code>代表python里特殊方法专用的标识，如<code>__init__（）</code>代表类的构造函数。</p>
<h3 id="特定命名方式"><a href="#特定命名方式" class="headerlink" title="特定命名方式"></a>特定命名方式</h3><p>主要是指 <code>__xxx__</code> 形式的系统保留字命名法。</p>
<p>项目中也可以使用这种命名，它的意义在于这种形式的变量是只读的，这种形式的类成员函数尽量不要重载。</p>
<p>如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span>(<span class="params">object</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self, id, parent = None</span>):</span></span><br><span class="line">    self.__id__ = id</span><br><span class="line">    self.__parent__ = parent</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__message__</span>(<span class="params">self, msgid</span>):</span></span><br><span class="line">    <span class="comment"># …</span></span><br></pre></td></tr></table></figure>

<p>其中 <code>__id__</code>、<code>__parent__</code> 和 <code>__message__</code> 都采用了系统保留字命名法<br> <code>__</code>双下划线开头双下划线结尾<code>__</code>：指那些包含在用户无法控制的命名空间中的“魔术”对象或属性<br> 如类成员的<code>__name__</code>、<code>__doc__</code>、<code>__init__</code>、<code>__import__</code>、<code>__file__</code>、等</p>
<p><strong>推荐永远不要将这样的命名方式应用于自己的变量或函数。</strong></p>
<p>单下划线结尾<code>_</code>：只是为了避免与python关键字的命名冲突</p>
<p><code>_</code>单下划线开头：弱“内部使用”标识，如：<code>from M import *</code>，将不导入所有以下划线开头的对象，包括包、模块、成员</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://zh-google-styleguide.readthedocs.io/en/latest/google-python-styleguide/python_style_rules/#comments">Google开源项目风格指南</a></p>
<p><a href="https://www.jianshu.com/p/a793c0d960fe">python命名规范</a></p>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>开始写博客</title>
    <url>/2020/07/15/%E5%BC%80%E5%8D%9A/</url>
    <content><![CDATA[<p>本博客用来记录一些学习笔记和工作中遇到的坑。</p>
<p><code>stay hungry, stay foolish</code></p>
<p><code>talk is cheap, show me the code</code></p>
<p>好了，第一篇文章就这样吧。</p>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
  </entry>
  <entry>
    <title>中文文本纠错</title>
    <url>/2020/09/27/%E4%B8%AD%E6%96%87%E6%96%87%E6%9C%AC%E7%BA%A0%E9%94%99/</url>
    <content><![CDATA[<img src="中文文本纠错/test.jpg" style="zoom:50%;" />

]]></content>
      <categories>
        <category>NLP</category>
      </categories>
      <tags>
        <tag>实体</tag>
        <tag>纠错</tag>
      </tags>
  </entry>
</search>
